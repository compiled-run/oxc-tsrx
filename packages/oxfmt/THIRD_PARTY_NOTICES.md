# Third-party notices

This package delegates ordinary source to the official `oxfmt` 0.59.0 npm
package, which retains its own license and notice files when installed.

The matching `@oxc-tsrx/native-*` package carries the notices for the linked
Rust/OXC binary distribution. `@oxc-tsrx/runtime` carries its JavaScript
runtime notice.
